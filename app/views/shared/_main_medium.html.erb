<div class="img-thumb first">
 <%= link_to article_path(article) do %> <div class="fbt-resize" style="background-image: url(<%= asset_path("#{article.image.preview.url}") %>)"></div><% end %>
  <div class="img-credits">
    <a class="post-category" href="#">Computing</a>
     <%= link_to article_path(article) do %> <h3>How can Build a Better Connection Between the Mind and Body</h3> <% end %>
    <div class="post-info">
      <span><%= article.created_at.strftime("%d %B %Y") %></span>
    </div>
  </div>
</div>
